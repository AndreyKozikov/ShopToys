package Model.Operations;

import Model.Interfaces.Operations;

public class ExitOperation implements Operations {
    @Override
    public boolean performOperation(Object... args) {
        return true;
    }
}
